import {createFileRoute} from '@tanstack/react-router'; import {ProfileDetail} from '@/components/caryandi/detail-pages';
export const Route=createFileRoute('/sellers/$sellerId')({head:()=>({meta:[{title:'Seller Profile — Caryandi'},{name:'description',content:'View vehicle inventory, seller verification and customer reviews.'},{property:'og:title',content:'Seller Profile — Caryandi'},{property:'og:description',content:'View vehicle inventory, seller verification and customer reviews.'},{property:'og:type',content:'profile'},{name:'twitter:card',content:'summary_large_image'}]}),component:()=>{const {sellerId}=Route.useParams();return <ProfileDetail kind="seller" id={sellerId}/>}});
