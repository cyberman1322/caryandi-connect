import {createFileRoute} from '@tanstack/react-router'; import {ProfileDetail} from '@/components/caryandi/detail-pages';
export const Route=createFileRoute('/agents/$agentId')({head:()=>({meta:[{title:'Import Agent Profile — Caryandi'},{name:'description',content:'View routes, services, pricing and reviews for a vehicle import agent.'},{property:'og:title',content:'Import Agent Profile — Caryandi'},{property:'og:description',content:'View routes, services, pricing and reviews for a vehicle import agent.'},{property:'og:type',content:'profile'},{name:'twitter:card',content:'summary_large_image'}]}),component:()=>{const {agentId}=Route.useParams();return <ProfileDetail kind="agent" id={agentId}/>}});
