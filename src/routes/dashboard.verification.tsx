import {createFileRoute} from '@tanstack/react-router'; import {DashboardPage} from '@/components/caryandi/dashboard-pages';
// ?vehicle=<id> opens that car's verification form (linked from a buyer's chat).
export const Route=createFileRoute('/dashboard/verification')({validateSearch:(s:Record<string,unknown>):{vehicle?:string}=>typeof s['vehicle']==='string'&&/^[0-9a-f-]{36}$/i.test(s['vehicle'])?{vehicle:s['vehicle']}:{},head:()=>({meta:[{title:'Verification — Caryandi Dashboard'},{name:'description',content:'Manage verification in your Caryandi workspace.'},{property:'og:title',content:'Verification — Caryandi Dashboard'},{property:'og:description',content:'Manage verification in your Caryandi workspace.'},{property:'og:type',content:'website'},{name:'twitter:card',content:'summary'}]}),component:()=> <DashboardPage page="verification"/>});
