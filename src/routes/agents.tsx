import {createFileRoute} from '@tanstack/react-router'; import {DirectoryPage} from '@/components/caryandi/listing-pages';
export const Route=createFileRoute('/agents')({head:()=>({meta:[{title:'Vehicle Import Agents — Caryandi Zambia'},{name:'description',content:'Compare vehicle import routes, services, pricing and verified agents.'},{property:'og:title',content:'Vehicle Import Agents — Caryandi Zambia'},{property:'og:description',content:'Compare vehicle import routes, services, pricing and verified agents.'},{property:'og:type',content:'website'},{name:'twitter:card',content:'summary_large_image'}]}),component:()=> <DirectoryPage type="agents"/>});
