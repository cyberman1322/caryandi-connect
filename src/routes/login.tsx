import {createFileRoute} from '@tanstack/react-router'; import {AuthPage} from '@/components/caryandi/auth-pages'; import {safeRedirectPath} from '@/lib/auth/redirect';
export const Route=createFileRoute('/login')({validateSearch:(search:Record<string,unknown>):{redirect?:string}=>{const redirect=safeRedirectPath(search['redirect']); return redirect?{redirect}:{}},head:()=>({meta:[{title:'Sign In — Caryandi'},{name:'description',content:'Sign in to your Caryandi account.'},{property:'og:title',content:'Sign In — Caryandi'},{property:'og:description',content:'Sign in to your Caryandi account.'},{property:'og:type',content:'website'},{name:'twitter:card',content:'summary'}]}),component:LoginRoute});
function LoginRoute(){const {redirect}=Route.useSearch(); return <AuthPage mode="login" redirect={redirect}/>}
