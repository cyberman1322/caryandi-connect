import {createFileRoute} from '@tanstack/react-router'; import {PartDetail} from '@/components/caryandi/detail-pages';
export const Route=createFileRoute('/parts/$partId')({head:()=>({meta:[{title:'Part Details — Caryandi'},{name:'description',content:'View automotive part condition, price, compatibility and seller information.'},{property:'og:title',content:'Part Details — Caryandi'},{property:'og:description',content:'View automotive part condition, price, compatibility and seller information.'},{property:'og:type',content:'website'},{name:'twitter:card',content:'summary_large_image'}]}),component:()=>{const {partId}=Route.useParams();return <PartDetail id={partId}/>}});
