import {useState} from 'react'; import {Link} from '@tanstack/react-router'; import {ArrowLeft,CheckCircle2,FileCheck,Heart,MapPin,MessageSquare,Phone,Share2,ShieldCheck,Star} from 'lucide-react'; import {Button} from '@/components/ui/button'; import {Badge} from '@/components/ui/badge'; import {Tabs,TabsContent,TabsList,TabsTrigger} from '@/components/ui/tabs'; import {Dialog,DialogContent,DialogDescription,DialogHeader,DialogTitle,DialogTrigger} from '@/components/ui/dialog'; import {Textarea} from '@/components/ui/textarea'; import {vehicles,services,agents,parts,reviews,firstOf} from '@/data/mock-data'; import {VehicleImage,ServiceImage} from './media'; import {Rating,VerifiedBadge,VehicleCard} from './cards';
export function VehicleDetail({id}:{id:string}){const v=vehicles.find(x=>x.id===id)??firstOf(vehicles); const [photo,setPhoto]=useState(v.image); const specs=[['Condition',v.condition],['Mileage',v.mileage],['Transmission',v.transmission],['Fuel type',v.fuel],['Engine size','2.0 L'],['Location',v.location],['Registration','Registered'],['Import status',v.id==='honda-grace-2018'?'Imported · duty cleared':'Local registration']]; return <main className="mx-auto max-w-7xl px-4 py-6 sm:px-6"><Link to="/vehicles" className="inline-flex items-center gap-1 text-sm text-muted-foreground hover:text-foreground"><ArrowLeft className="size-4"/>Back to vehicles</Link><div className="mt-5 grid gap-8 lg:grid-cols-[minmax(0,1.5fr)_minmax(320px,.7fr)]"><div><VehicleImage index={photo} alt={v.name} className="aspect-[16/10] rounded-lg"/><div className="mt-3 grid grid-cols-4 gap-2">{[v.image,(v.image+1)%4,(v.image+2)%4,(v.image+3)%4].map((x,i)=><button key={i} onClick={()=>setPhoto(x)} className={`overflow-hidden rounded-md border-2 ${photo===x?'border-primary':'border-transparent'}`}><VehicleImage index={x} className="aspect-[4/3]"/></button>)}</div><Tabs defaultValue="overview" className="mt-8"><TabsList><TabsTrigger value="overview">Overview</TabsTrigger><TabsTrigger value="documents">Documents</TabsTrigger><TabsTrigger value="verification">Verification</TabsTrigger></TabsList><TabsContent value="overview"><div className="mt-4 grid gap-px overflow-hidden rounded-lg border bg-border sm:grid-cols-2">{specs.map(([k,val])=><div key={k} className="flex justify-between bg-card p-4 text-sm"><span className="text-muted-foreground">{k}</span><b>{val}</b></div>)}</div><div className="mt-6"><h2 className="text-xl font-semibold">Seller's description</h2><p className="mt-2 leading-7 text-muted-foreground">Well maintained vehicle with complete service history. Clean interior, smooth drive and available for inspection by appointment.</p></div></TabsContent><TabsContent value="documents"><div className="mt-4 space-y-3 rounded-lg border p-5">{['Registration certificate','Road tax status','Import declaration'].map(x=><div className="flex items-center gap-3" key={x}><FileCheck className="size-5 text-success"/><span>{x}</span><Badge className="ml-auto" variant="outline">Provided</Badge></div>)}</div></TabsContent><TabsContent value="verification"><div className="mt-4 rounded-lg border bg-success/5 p-5"><div className="flex gap-3"><ShieldCheck className="size-6 text-success"/><div><h3 className="font-semibold">Identity and listing details checked</h3><p className="mt-1 text-sm text-muted-foreground">Verification indicators help you assess a listing, but always inspect a vehicle and its documents before payment.</p></div></div></div></TabsContent></Tabs></div><aside><div className="sticky top-24 rounded-lg border bg-card p-5 shadow-sm"><div className="flex items-start justify-between"><div><Badge variant="outline">{v.year}</Badge><h1 className="mt-3 text-2xl font-bold">{v.name}</h1><p className="mt-2 text-3xl font-bold">K{v.price.toLocaleString()}</p></div><div className="flex"><Button variant="ghost" size="icon" aria-label="Share"><Share2/></Button><Button variant="ghost" size="icon" aria-label="Save"><Heart/></Button></div></div><p className="mt-4 flex items-center gap-2 text-sm text-muted-foreground"><MapPin className="size-4"/>{v.location}</p><div className="my-5 border-t"/><div className="flex items-center justify-between"><div><p className="font-semibold">{v.seller}</p><p className="text-sm text-muted-foreground">Responds in about 1 hour</p></div>{v.verified&&<VerifiedBadge/>}</div><Dialog><DialogTrigger asChild><Button className="mt-5 w-full"><MessageSquare/>Send enquiry</Button></DialogTrigger><DialogContent><DialogHeader><DialogTitle>Contact {v.seller}</DialogTitle><DialogDescription>Your message is a preview and will not be sent.</DialogDescription></DialogHeader><Textarea defaultValue={`Hi, is the ${v.name} still available?`}/><Button><MessageSquare/>Preview enquiry</Button></DialogContent></Dialog><Button variant="outline" className="mt-2 w-full"><Phone/>Show contact</Button><p className="mt-4 text-center text-xs text-muted-foreground">Never send money before viewing the vehicle and confirming ownership.</p></div></aside></div><section className="mt-12"><h2 className="text-2xl font-bold">Similar vehicles</h2><div className="mt-5 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{vehicles.filter(x=>x.id!==v.id).slice(0,3).map(x=><VehicleCard key={x.id} v={x}/>)}</div></section></main>}
export function ProfileDetail({kind,id}:{kind:'service'|'agent'|'seller';id:string}){const item=kind==='agent'?(agents.find(x=>x.id===id)??firstOf(agents)):kind==='seller'?{...firstOf(services.slice(1)),name:id==='autoworld-zambia'?'Autoworld Zambia':'Copperbelt Motors',type:'Vehicle dealer',detail:'A trusted selection of inspected vehicles and straightforward support.'}:(services.find(x=>x.id===id)??firstOf(services)); return <main><div className="bg-muted/35"><div className="mx-auto grid max-w-7xl gap-6 px-4 py-10 sm:px-6 md:grid-cols-[220px_1fr_auto] md:items-end"><ServiceImage index={kind==='agent'?3:item.image} className="aspect-[4/3] rounded-lg" alt={item.name}/><div><p className="text-sm font-semibold text-primary">{item.type}</p><h1 className="mt-1 text-3xl font-bold">{item.name}</h1><p className="mt-2 flex items-center gap-1 text-muted-foreground"><MapPin className="size-4"/>{item.location}</p><div className="mt-3 flex items-center gap-3"><Rating value={item.rating} count={item.reviews}/>{item.verified&&<VerifiedBadge/>}</div></div><Button><MessageSquare/>Contact</Button></div></div><div className="mx-auto grid max-w-7xl gap-8 px-4 py-8 sm:px-6 lg:grid-cols-[1fr_340px]"><div><h2 className="text-xl font-semibold">About</h2><p className="mt-2 leading-7 text-muted-foreground">{item.detail}. Clear pricing, helpful communication and service for customers across Zambia.</p><h2 className="mt-8 text-xl font-semibold">{kind==='agent'?'Routes and services':kind==='seller'?'Available inventory':'Services offered'}</h2><div className="mt-4 grid gap-3 sm:grid-cols-2">{(kind==='agent'?['Japan → Zambia','Durban → Zambia','Dar es Salaam → Zambia','Customs & documentation']:kind==='seller'?['Vehicle inspections','Trade-in support','Finance referrals','After-sales assistance']:['Computer diagnostics','Engine service','Brake repair','Pre-purchase inspection']).map((x,i)=><div key={x} className="flex gap-3 rounded-lg border p-4"><CheckCircle2 className="size-5 text-success"/><div><b>{x}</b><p className="mt-1 text-sm text-muted-foreground">{i%2?'Available by appointment':'Available this week'}</p></div></div>)}</div><h2 className="mt-8 text-xl font-semibold">Customer reviews</h2><div className="mt-4 space-y-3">{reviews.map(r=><article key={r.name} className="rounded-lg border p-4"><div className="flex justify-between"><b>{r.name}</b><span className="flex">{Array.from({length:r.rating}).map((_,i)=><Star key={i} className="size-4 fill-warning text-warning"/>)}</span></div><p className="mt-2 text-sm text-muted-foreground">{r.text}</p></article>)}</div></div><aside className="rounded-lg border bg-card p-5 self-start"><h2 className="font-semibold">At a glance</h2><dl className="mt-4 space-y-4 text-sm"><div className="flex justify-between"><dt className="text-muted-foreground">Starting price</dt><dd className="font-semibold">{item.price}</dd></div><div className="flex justify-between"><dt className="text-muted-foreground">Availability</dt><dd className="font-semibold">Open today</dd></div><div className="flex justify-between"><dt className="text-muted-foreground">Response time</dt><dd className="font-semibold">Within 2 hours</dd></div></dl><Button className="mt-5 w-full">Request service</Button><Button variant="outline" className="mt-2 w-full"><Phone/>Contact</Button></aside></div></main>}
export function PartDetail({id}:{id:string}){const p=parts.find(x=>x.id===id)??firstOf(parts); return <main className="mx-auto max-w-6xl px-4 py-8 sm:px-6"><Link to="/parts" className="text-sm text-muted-foreground">← Back to parts</Link><div className="mt-5 grid gap-8 md:grid-cols-2"><ServiceImage index={2} className="aspect-square rounded-lg" alt={p.name}/><div><div className="flex gap-2"><Badge>{p.category}</Badge><Badge variant="outline">{p.condition}</Badge></div><h1 className="mt-4 text-3xl font-bold">{p.name}</h1><p className="mt-3 text-3xl font-bold">K{p.price.toLocaleString()}</p><p className="mt-4 flex items-center gap-2 text-muted-foreground"><MapPin className="size-4"/>{p.location}</p><div className="my-6 border-t"/><h2 className="font-semibold">Sold by {p.seller}</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">Part fitment should be confirmed using your vehicle details before purchase.</p><Button className="mt-6 w-full">Contact seller</Button><Button variant="outline" className="mt-2 w-full">Ask about compatibility</Button></div></div></main>}
